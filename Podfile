platform :ios, '15.0'

target 'whooppe' do
  use_frameworks!
  
  # Firebase - Required for notifications
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  
  # Image loading (optional but recommended)
  # pod 'Kingfisher'
  
  # Payment gateway
  # pod 'Razorpay'
end